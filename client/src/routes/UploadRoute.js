import UploadForm from "../components/UploadForm";

export default function Upload(){
    return(
        <main style={{ padding: "1rem 0" }}>
        <h2>Upload one Item</h2>
        <UploadForm></UploadForm>
      </main>
    );
}