import SigninForm from "../components/SignInForm";

export default function Signin(){
    return(
        <main style={{ padding: "1rem 0" }}>
        <h2>Sign in (form)</h2>
        <SigninForm />
      </main>
    );
}